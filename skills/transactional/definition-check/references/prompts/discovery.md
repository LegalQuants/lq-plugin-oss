Scan every fragment for plausible contractual labels that may need semantic review. Be over-inclusive and do not decide whether a proposal is actually defined. Identify source text by fragment number and exact zero-based start/end offsets, preserve its spelling, and give a short note. The supervisor maps those coordinates back to the source document. Write only the JSON object described by response_schema. Start from response_template and append rows shaped as [fragment, start, end, term, note]. Do not add other keys or prose. Every fragment number must be one of the integers allowed by the row schema.
