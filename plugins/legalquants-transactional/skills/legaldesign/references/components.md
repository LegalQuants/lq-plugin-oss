# Component registry

Generated by `scripts/scaffold.py components --markdown` from [`components.json`](components.json). Do not edit this table by hand.

## Recommended for new designs

Choose the relationship first. These components are the curated starting set, not a requirement to draw a diagram.

| Runtime ID | Relationships | Capacity | Parameters | Detail |
|---|---|---|---|---|
| `flow` | sequence | 2–6 stages | `stages` 2–6 list; fields title, sub, detail, accentRole; `edges` 1–5 strings | yes |
| `timeline` | time | 3–5 marks | `marks` 3–5 list; fields when, title, sub, detail; `nowAt` optional index; `orientation` optional: horizontal \| vertical | yes |
| `hierarchy` | hierarchy, containment | 2–4 branches; 1–2 leaves per branch | `root` required text; `rootSub` optional text; `rootDetail` optional text; `branches` 2–4 list; fields title, sub, detail, accentRole, edge, leaves | yes |
| `zones` | containment | 1–3 items in each of 2 zones | `zoneA` required text; `itemsA` 1–3 list; fields title, sub, detail; `zoneB` required text; `itemsB` 1–3 list; fields title, sub, detail; `crossing` required text | yes |
| `beforeAfter` | change | 2 states | `orientation` optional: vertical \| horizontal; `beforeLabel` required text; `beforeSub` required text; `edge` required text; `afterLabel` required text; `afterSub` required text | no |
| `hub` | interaction, convergence | 3–6 spokes | `centre` required text; `spokes` 3–6 list; fields title, edge, detail; `toward` required: centre \| satellites | yes |
| `compareTwo` | comparison | 2 options across 2–4 rows | `a` required text; `b` required text; `aDetail` optional text; `bDetail` optional text; `highlight` required: a \| b \| none; `rows` 2–4 list; fields label, a, b, winner | yes |
| `rungBars` | comparison, quantity | 2–8 bars | `items` 2–8 list; fields label, value, open, detail; `unit` required text; `yAxisTitle` required text; `sourceNote` required text | yes |
| `hairlineLine` | time, change | 3–30 points | `points` 3–30 list; fields x, y, label, open, detail; `xLabels` 3–30 strings; `xAxisTitle` required text; `yAxisTitle` required text; `unit` required text; `baselineZero` optional: yes \| no; `sourceNote` required text | yes |
| `stackedRungs` | part-of-whole, comparison | 2–6 bars × 2–4 parts | `partLabels` 2–4 strings; `items` 2–6 list; fields label, parts, open, detail; `unit` required text; `xAxisTitle` required text; `sourceNote` required text | yes |
| `rangeBars` | quantity, comparison | 1–8 intervals | `items` 1–8 list; fields label, low, high, mark, markLabel, open, detail; `unit` required text; `xAxisTitle` required text; `scaleMin` optional number; `scaleMax` optional number; `sourceNote` required text | yes |

## Saved-file compatibility

The following renderers remain available to open existing artifacts and explicit template imports, but are not offered for new compositions. Full parameter contracts remain in `components.json`.

`funnel`, `loop`, `matrix`, `figurePath`, `stairSteps`, `pyramid`, `gears`, `hairlineArea`, `tickDonut`, `tickRows`, `pairedRungs`, `plumbScatter`, `rungWaterfall`, `dotHeat`, `tickGauge`, `dumbbell`, `treemap`, `rungHistogram`, `tickBox`, `streamRibbon`.

The runtime carries no third-party code and copies nothing from Lieflat Charts (PolyForm Noncommercial).

Unknown component IDs, undocumented fields, and over-capacity lists fail validation.
