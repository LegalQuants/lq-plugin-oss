{
  "name": "@lq/legaldesign-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build-assets.mjs",
    "check": "node build-assets.mjs --check",
    "test:browser": "node browser/run.mjs"
  },
  "dependencies": {
    "esbuild": "^0.28.2"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "ajv": "^8.20.0"
  }
}
